\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage[section]{placeins}

\title{Zeta-Mass Closure Operator: Perfect Closure as a Shared Spectral Eigenvalue Law}
\author{John Van Geem / RQM Technologies}
\date{April 2026}

\begin{document}
\maketitle

\begin{abstract}
We formulate a compatibility operator that transports closure labels between zeta residual language and mass-shell norm language. We define $\mathcal A_{\mathbf u}=\mathbf u(\rho\,d/d\rho+1/2)$, identify eigenmodes $\chi_t(\rho)=\rho^{-1/2-\mathbf u t}$, and show $\mathcal A_{\mathbf u}\chi_t=t\chi_t$. We then apply the completed residual through $\Xi(\mathcal A_{\mathbf u})$, so that $\Xi(t_n)=0$ implies kernel selection $\Xi(\mathcal A_{\mathbf u})\chi_{t_n}=0$. Finally, with conversion scale $L_*$, we write $m_n=(\hbar/(cL_*))|t_n|$ and $P\bar P=(\hbar t_n/L_*)^2$. This establishes a bridge of compatibility, not an identity between zeta zeros and particles.
\end{abstract}

\noindent\textbf{Keywords:} closure operator; dilation generator; completed zeta; spectral eigenmode; mass shell; scale conversion.

\section{Introduction}
Paper 4 is the synthesis step. We keep one spectral label $t_n$ and track how it appears in three settings: completed residual vanishing, operator eigenmodes, and mass-shell norms. The intent is formal transport consistency, not physical equivalence.

\section{Contributions}
\begin{enumerate}
  \item We define a quaternionic-oriented dilation generator $\mathcal A_{\mathbf u}$.
  \item We show why the $+1/2$ shift is required for clean eigenvalue extraction on $\chi_t$.
  \item We define the selector $\Xi(\mathcal A_{\mathbf u})$ and derive kernel condition induced by $\Xi(t_n)=0$.
  \item We give the scale map from dimensionless ordinates to mass-shell norm and isolate the role of $L_*$.
\end{enumerate}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.88\textwidth]{../assets/figures/zeta-to-mass-shell-pipeline.svg}
\caption{Compatibility map from scalar closure condition to operator kernel and mass-shell norm realization.}
\end{figure}

\section{Mathematical Setup and Formal Result}
Define
\[
\mathcal A_{\mathbf u}=\mathbf u\left(\rho\frac{d}{d\rho}+\frac12\right),\qquad \mathbf u^2=-1,\qquad \rho>0.
\]
Set
\[
\chi_t(\rho)=\rho^{-1/2-\mathbf u t}.
\]
Then
\[
\rho\frac{d}{d\rho}\chi_t=\left(-\frac12-\mathbf u t\right)\chi_t,
\]
so
\[
\left(\rho\frac{d}{d\rho}+\frac12\right)\chi_t=-\mathbf u t\,\chi_t,
\]
and hence
\[
\mathcal A_{\mathbf u}\chi_t=t\chi_t.
\]

Now define
\[
\Xi(t)=\xi\!\left(\frac12+it\right),\qquad \mathcal Z_{\mathbf u}=\Xi(\mathcal A_{\mathbf u}).
\]
Functional calculus gives
\[
\Xi(\mathcal A_{\mathbf u})\chi_t=\Xi(t)\chi_t.
\]
Therefore, if $\Xi(t_n)=0$, then
\[
\Xi(\mathcal A_{\mathbf u})\chi_{t_n}=0.
\]

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{../assets/figures/closure-operator-bridge.svg}
\caption{Operator pathway from scale modes to kernel selection under $\Xi(\mathcal A_{\mathbf u})$.}
\end{figure}

\section{Mass-Shell Realization and Scale Law}
Use
\[
P\bar P=m^2c^2.
\]
Introduce conversion length $L_*$ by $\log\rho=\ell/L_*$. Then
\[
m_n=\frac{\hbar}{cL_*}|t_n|,
\]
with equivalent forms
\[
P\bar P=\left(\frac{\hbar t_n}{L_*}\right)^2,
\]
\[
L_*=t_n\frac{\hbar}{mc}=t_n\bar\lambda_C.
\]
Fixed $L_*$ yields predictive structure; freely varying $L_*$ yields descriptive fits.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.84\textwidth]{../assets/figures/tn-two-faces-bridge.svg}
\caption{The same ordinate $t_n$ seen as closure trace ($\Xi(t_n)=0$) and mass-shell norm realization.}
\end{figure}

\section{Interpretation and Discussion}
This map is a compatibility bridge. It aligns scalar closure labels with operator kernels and norm equations, but it does not identify zeta zeros with particles. The $+1/2$ shift aligns mode exponents with half-line structure from Papers 1 and 2.

\section{Interpretive Boundaries}
\begin{itemize}
  \item We do \textbf{not} claim a proof of RH.
  \item We do \textbf{not} claim zeta zeros are particles.
  \item We do \textbf{not} claim universal $L_*$ has been established here.
  \item We do \textbf{not} claim derivation of Standard Model masses.
\end{itemize}

\section{Conclusion}
We constructed the zeta-mass closure operator bridge in three steps: eigenmode definition, completed-residual kernel transport, and scale-converted mass-shell realization. The framework shows shared spectral labeling under explicit assumptions and preserves non-claim boundaries. Paper 5 determines whether $L_*$ can be fixed independently enough for out-of-sample predictive tests.

\section*{References}
\begin{enumerate}
  \item E. C. Titchmarsh and D. R. Heath-Brown, \emph{The Theory of the Riemann Zeta-Function}, 1986.
  \item H. M. Edwards, \emph{Riemann's Zeta Function}, 2001.
  \item S. L. Adler, \emph{Quaternionic Quantum Mechanics and Quantum Fields}, 1995.
\end{enumerate}

\end{document}
